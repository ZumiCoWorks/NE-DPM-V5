# Copy this file to ".env" and fill in the real values (do NOT commit .env)

# 1. Get this from Supabase Settings -> API -> Project URL
SUPABASE_URL=https://your-project-id.supabase.co

# 2. Get this from Supabase Settings -> API -> service_role (Secret)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# 3. A secret for signing tokens (used by server). Generate locally with:
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=replace-with-a-long-random-hex

# 4. Frontend (Vite) values
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key-here

# Notes:
# - Do NOT commit the real values. This repository's .gitignore already ignores .env files.
# - SUPABASE_SERVICE_ROLE_KEY is a secret that grants admin privileges to your Supabase project.
# - After filling in .env, restart the server (npm run dev or npm run server:dev).
