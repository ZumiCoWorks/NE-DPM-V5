{
  "version": 2,
  "buildCommand": "cd dpm-web && npm run build",
  "outputDirectory": "dpm-web/dist",
  "installCommand": "cd dpm-web && npm install",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}